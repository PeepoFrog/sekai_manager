package init

import (
	"github.com/PeepoFrog/sekai_manager/src/types"
)

func InitInstance(mnemonic []byte) (*types.InstanceConfig, error) {
	// Download or check if exists required version
	// New or Join
	// apply config

	return &types.InstanceConfig{}, nil
}
